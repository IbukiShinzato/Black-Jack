package main

type Player struct {
	name  string
	money int
}

type Dealer struct {
	name  string
	money int
}

// func (d *Dealer) shuffle(p Player) {
// 	p.deck = append(p.deck)
// 	d.deck = append(d.deck)
// }
